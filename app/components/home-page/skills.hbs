<h1>skills</h1>

<div class="row justify-content-center">
  <div class="col-auto">
  <table class="table">
    <tbody>
      {{#each skills as |skill|}}
        <tr>
          <td>
            <p class="skill-label text-nowrap">{{skill.name}}</p>
          </td>
          <td>
            {{#each (repeat skill.level) as |empty|}}
              <div class="bullet bg-info"></div>
            {{/each}}
          </td>
        </tr>
        {{/each}}
      </tbody>
  </table>
</div>
</div>
